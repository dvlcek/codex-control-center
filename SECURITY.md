# Security

Please avoid publishing exploitable security issues in a public issue before a fix is available.

This project intentionally keeps the renderer sandboxed, exposes only narrow IPC methods, and avoids sending local Codex session data to a remote service.

If you maintain a public fork, configure a private security-reporting channel in the repository settings and document it here.
